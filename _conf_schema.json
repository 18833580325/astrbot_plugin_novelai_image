{
  "api_key": {
    "description": "NovelAI API Key",
    "type": "string",
    "hint": "填写 NovelAI token/API Key，不需要 Bearer 前缀。",
    "default": ""
  },
  "base_url": {
    "description": "NovelAI 生成接口",
    "type": "string",
    "hint": "官方接口通常使用 https://image.novelai.net/ai/generate-image 或 https://api.novelai.net/ai/generate-image；也可填兼容 wrapper 地址。",
    "default": "https://image.novelai.net/ai/generate-image"
  },
  "proxy_url": {
    "description": "HTTP 代理",
    "type": "string",
    "hint": "服务器 Docker 内访问宿主 mihomo 可填 http://172.17.0.1:7890；留空则不使用代理。",
    "default": ""
  },
  "model": {
    "description": "默认模型",
    "type": "string",
    "options": [
      "nai-diffusion-4-5-full",
      "nai-diffusion-4-5-curated",
      "nai-diffusion-4-full",
      "nai-diffusion-4-curated-preview",
      "nai-diffusion-3",
      "nai-diffusion-furry-3"
    ],
    "default": "nai-diffusion-4-5-full"
  },
  "width": {
    "description": "默认宽度",
    "type": "int",
    "hint": "建议使用 64 的倍数。",
    "default": 832
  },
  "height": {
    "description": "默认高度",
    "type": "int",
    "hint": "建议使用 64 的倍数。",
    "default": 1216
  },
  "steps": {
    "description": "默认步数",
    "type": "int",
    "default": 28
  },
  "scale": {
    "description": "默认相关性 Scale",
    "type": "float",
    "default": 5.5
  },
  "seed": {
    "description": "默认种子",
    "type": "int",
    "hint": "-1 表示每次随机。",
    "default": -1
  },
  "sampler": {
    "description": "默认采样器",
    "type": "string",
    "options": [
      "k_euler_ancestral",
      "k_euler",
      "k_dpmpp_2m",
      "k_dpmpp_2s_ancestral",
      "ddim"
    ],
    "default": "k_euler_ancestral"
  },
  "negative_prompt": {
    "description": "默认负面提示词",
    "type": "string",
    "default": "nsfw, lowres, bad anatomy, bad hands, text, error, missing fingers, extra digit, fewer digits, cropped, worst quality, low quality, normal quality, jpeg artifacts, signature, watermark, username, blurry"
  },
  "timeout_seconds": {
    "description": "请求超时时间",
    "type": "int",
    "default": 180
  },
  "llm_prompt_optimizer_prompt": {
    "description": "LLM 提示词优化指令",
    "type": "string",
    "hint": "用户在 /nai 命令中加入 -llm 时，会用当前会话大模型按这段指令优化提示词。",
    "default": "你是 NovelAI 提示词优化器。把用户输入改写为适合 NovelAI/anime diffusion 的英文提示词。要求：只输出最终英文 prompt，不要 Markdown，不要解释。保留用户的主体、动作、服装、场景、构图和风格要求。如果用户输入中文，请准确翻译并补充常用英文 tag。避免加入用户没有要求的露骨色情、未成年、血腥、政治或违法内容。输出以逗号分隔的 tag/prompt。"
  },
  "vision_review_enabled": {
    "description": "启用生成后视觉审核",
    "type": "bool",
    "hint": "开启后，图片会先发给有视觉能力的大模型审核，通过后才发送到聊天。",
    "default": false
  },
  "vision_review_mode": {
    "description": "视觉审核模型来源",
    "type": "string",
    "options": ["astrbot_caption", "astrbot_current", "custom_openai", "off"],
    "hint": "astrbot_caption 使用 AstrBot 的图片描述模型；astrbot_current 使用当前会话模型；custom_openai 使用下面单独填写的 OpenAI 兼容接口。",
    "default": "astrbot_caption"
  },
  "vision_base_url": {
    "description": "视觉审核接口",
    "type": "string",
    "hint": "仅 custom_openai 模式需要填写。填写 OpenAI 兼容的 chat/completions 地址，例如 https://api.openai.com/v1/chat/completions 或你的中转地址。",
    "default": ""
  },
  "vision_api_key": {
    "description": "视觉审核 API Key",
    "type": "string",
    "hint": "仅 custom_openai 模式需要填写，不需要 Bearer 前缀。",
    "default": ""
  },
  "vision_model": {
    "description": "视觉审核模型",
    "type": "string",
    "hint": "仅 custom_openai 模式需要填写。填写支持图片输入的模型，例如 gpt-4o、gpt-4.1、gemini 等中转支持的模型名。",
    "default": ""
  },
  "vision_proxy_url": {
    "description": "视觉审核代理",
    "type": "string",
    "hint": "留空时会复用 NovelAI 的 proxy_url；不需要代理可留空且保持 proxy_url 为空。",
    "default": ""
  },
  "vision_timeout_seconds": {
    "description": "视觉审核超时时间",
    "type": "int",
    "default": 120
  },
  "vision_review_fail_closed": {
    "description": "审核失败时禁止发送",
    "type": "bool",
    "hint": "开启后，如果视觉审核接口报错或超时，图片不会发送。关闭则审核失败时放行。",
    "default": false
  },
  "vision_block_reply": {
    "description": "视觉审核拦截回复",
    "type": "string",
    "hint": "发送到群里或私聊里的简短提示；具体拦截原因只会写入后台日志。",
    "default": "您生成的内容被拦截。"
  },
  "vision_review_prompt": {
    "description": "视觉审核提示词",
    "type": "string",
    "hint": "要求模型只返回 JSON：{\"allow\": true/false, \"reason\": \"...\"}。",
    "default": "你是图片发送前的审核器。请判断图片是否适合发送到聊天群。重点拦截：未成年人或疑似未成年色情、真实人物色情、血腥暴力、违法内容、明显仇恨或极端内容。如果只是成人二次元性感内容，且没有未成年特征，可以允许。只返回 JSON，不要返回 Markdown。格式：{\"allow\": true, \"reason\": \"\"}。"
  }
}
